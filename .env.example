# Leave unset to train a fresh model in-process at startup (a few seconds
# on CPU, using the synthetic series in app/data.py). Set to a file path to
# load persisted weights from there if present, or save freshly trained
# weights there for next time.
MODEL_PATH=
